#!/bin/bash

# This script prepares the speech-to-text models.
# Add your model preparation commands below.

echo "Preparing speech-to-text models..."
# Example command to download models
# wget http://example.com/path/to/model -O model.zip

# Example command to unzip models
# unzip model.zip -d ./models

echo "Speech-to-text models prepared."